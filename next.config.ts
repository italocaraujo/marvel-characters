/** @type {import('next').NextConfig} */
const nextConfig = {
  compiler: {
    styledComponents: true,
  },
};

module.exports = {
  eslint: {
    ignoreDuringBuilds: true,
  },
};

module.exports = nextConfig;
